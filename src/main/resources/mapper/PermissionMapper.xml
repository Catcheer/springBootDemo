<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.springbootdemo.mapper.PermissionMapper">

    <select id="findPermissionsByCondition" resultType="com.example.springbootdemo.model.Permission">
        SELECT * FROM `sys_permission`
        <where>
            <if test="query.permissionName != null and query.permissionName != ''">AND permission_name LIKE CONCAT('%', #{query.permissionName}, '%')</if>
            <if test="query.permissionCode != null and query.permissionCode != ''">AND permission_code LIKE CONCAT('%', #{query.permissionCode}, '%')</if>
        </where>
        ORDER BY id DESC
        LIMIT #{size} OFFSET #{offset}
    </select>

    <select id="countPermissionsByCondition" resultType="long">
        SELECT COUNT(*) FROM `sys_permission`
        <where>
            <if test="query.permissionName != null and query.permissionName != ''">AND permission_name LIKE CONCAT('%', #{query.permissionName}, '%')</if>
            <if test="query.permissionCode != null and query.permissionCode != ''">AND permission_code LIKE CONCAT('%', #{query.permissionCode}, '%')</if>
        </where>
    </select>

</mapper>